<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lend.chat.dao.BaseChatDao">
  <select id="selectAllSql"  parameterType="java.lang.String" resultType="map">
   ${selSql}
  </select>
  
   <select id="executeSql"  parameterType="java.lang.String" resultType="Integer">
   ${excSql}
  </select>
  
   <select id="selectCountSql"  parameterType="java.lang.String" resultType="Integer">
   ${cunSql}
  </select>
</mapper>